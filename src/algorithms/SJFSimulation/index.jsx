import React, { useState } from "react";
import "../style.css";

function SJFSimulation({ processData, overhead }) {
  const [simulationData, setSimulationData] = useState([]);

  return (<></>)
}

export default SJFSimulation;
