import React, { useState } from "react";
import "../style.css";

function EDFSimulation({ processData, overhead }) {
  const [simulationData, setSimulationData] = useState([]);

  return (<></>)
}

export default EDFSimulation;
